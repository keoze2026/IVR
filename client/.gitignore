# Scoped to client/ deliberately.
#
# A root .gitignore listing `.env` and `*.pyc` would contradict IVR/, which
# tracks both today. Fixing that is the backend's call — see
# ../docs/BACKEND-REQUIREMENTS.md §0.

node_modules/
dist/
.vite/
*.tsbuildinfo

.env
.env.*
!.env.example

coverage/
*.log
.DS_Store
Thumbs.db
