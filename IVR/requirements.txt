# --- Core -------------------------------------------------------------
Django==5.2.4
djangorestframework==3.15.2
django-filter==24.3
drf-spectacular==0.27.2

# --- Async / realtime --------------------------------------------------
channels==4.1.0
channels-redis==4.2.0
daphne==4.1.2
uvicorn[standard]==0.30.6
gunicorn==22.0.0

# --- Task queue --------------------------------------------------------
celery==5.4.0
# 2.7.x pins Django<5.2 and cannot be used with the 5.2 LTS the spec requires.
django-celery-beat==2.9.0
redis==5.0.8
django-redis==5.4.0

# --- Data --------------------------------------------------------------
# 3.2.1 has no cp313 binary wheel; 3.2.2 is the first with one.
psycopg[binary,pool]==3.2.13

# --- Telephony / contacts ---------------------------------------------
phonenumbers==8.13.44
twilio==9.2.3
requests==2.32.3
PyNaCl==1.5.0          # Telnyx webhook Ed25519 signature verification

# --- Storage / TTS -----------------------------------------------------
boto3==1.34.162

# --- Ops ---------------------------------------------------------------
sentry-sdk[django,celery]==2.13.0
django-prometheus==2.3.1
flower==2.0.1
holidays==0.55

# --- Dev / test --------------------------------------------------------
pytest==8.3.2
pytest-django==4.9.0
factory-boy==3.3.1
freezegun==1.5.1
# The [lua] extra pulls in lupa. Only needed if you run the limiter tests
# without a real Redis; the suite prefers a live one (see tests/conftest.py).
fakeredis[lua]==2.24.1
ruff==0.6.3
